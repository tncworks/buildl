# Vendored package

Copied from `somnia-chain/dreamdex-bot-kit` at `packages/ec-core` (clone of `main`, 2026-09-03).
License: MIT (see LICENSE). Reason: the package is `private: true` and not published to npm
(`npm view @dreamdex-bot-kit/ec-core` returns 404).

Changes from upstream: `package.json` name and pinned SDK version only. `src/` and `tests/` are byte-identical to upstream.

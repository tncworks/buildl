import { H1, Note } from "@/components/ui";

export default function DeployPage() {
  return (
    <>
      <H1 sub="Generate a testnet burner wallet, fund it, and run a tuned strategy live.">Deploy</H1>
      <Note>Built in step 7 of the plan. Not available yet.</Note>
    </>
  );
}

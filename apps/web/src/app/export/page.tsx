import { H1, Note } from "@/components/ui";

export default function ExportPage() {
  return (
    <>
      <H1 sub="Export a tuned strategy as a Bot Kit configuration for mainnet operators.">Export</H1>
      <Note>Built in step 8 of the plan. Not available yet.</Note>
    </>
  );
}
